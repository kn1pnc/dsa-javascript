import { circleArea, squareArea } from "./calcArea.js";

const r = 4;
const s = 70;

console.log(`para r = ${r}, a área do círculo é: ${circleArea(4)}`);
console.log(`para s = ${s}, a área do quadrado é: ${squareArea(s)}`);